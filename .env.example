# Environment Variables — BibleSaaS
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# =============================================================
# SUPABASE
# =============================================================
# Found in: Supabase project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — server-side only, NEVER expose to client
# Used by: background jobs, seed scripts, admin operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================
# ANTHROPIC (Claude)
# =============================================================
# https://console.anthropic.com → API Keys
# Models used: claude-sonnet-4-6 (primary), claude-haiku-4 (bulk jobs)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# =============================================================
# STRIPE (Payments)
# =============================================================
# https://dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_test_your-key-here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-key-here

# Stripe webhook secret — from: stripe listen → output or dashboard webhook endpoint
# Required for: subscription lifecycle events, gift purchases
STRIPE_WEBHOOK_SECRET=whsec_your-secret-here

# =============================================================
# RESEND (Email)
# =============================================================
# https://resend.com → API Keys
# Used for: magic links, weekly Charles letters, Year in Review PDF delivery
RESEND_API_KEY=re_your-key-here

# The verified sending domain (configure in Resend dashboard)
RESEND_FROM_EMAIL=your-edition@yourdomain.com

# =============================================================
# ESV BIBLE API
# =============================================================
# https://api.esv.org → Request API key (free for development)
# ESV text cached with 24-hour TTL per license terms
# LICENSE NOTICE: ESV text must not be permanently cached; requires LLC registration for commercial use
ESV_API_KEY=your-esv-api-key-here

# =============================================================
# API.BIBLE
# =============================================================
# https://scripture.api.bible → My Apps → Create App → API Key
# Used for: NIV, NASB, NLT, CSB (Standard tier+ only)
# Short cache only (1h); never permanently stored per license terms
API_BIBLE_KEY=your-api-bible-key-here

# =============================================================
# APPLICATION CONFIG
# =============================================================
# Public-facing URL of the deployed app (no trailing slash)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# =============================================================
# OPTIONAL: BACKGROUND JOBS
# =============================================================
# Secret key for authenticating cron job webhook calls from Vercel/external scheduler
# Generate with: openssl rand -hex 32
CRON_SECRET=your-random-secret-here
